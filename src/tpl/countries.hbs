<div>
    <h1>{{name}}</h1>
    <h2>Capital: <span>{{capital}}</span></h2>
    <h2>Population: <span>{{population}}</span></h2>
    <h2>Languages:</h2>
    <ul>
        {{#each languages}}
        <li>{{this.name}}</li>
        {{/each}}
    </ul>
</div>
<div>
    <img src={{flag}} alt={{cioc}}>
</div>